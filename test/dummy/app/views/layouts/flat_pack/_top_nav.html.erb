<%= render FlatPack::TopNav::Component.new do |nav| %>
  <% nav.left do %>
    <%= render FlatPack::Button::Component.new(
      icon: "menu",
      icon_only: true,
      style: :ghost,
      size: :md,
      aria: {label: "Toggle sidebar"},
      data: {
        "flat-pack--sidebar-layout-target": "mobileToggle",
        action: "click->flat-pack--sidebar-layout#toggleMobile"
      },
      class: "md:hidden"
    ) %>

  <% end %>

  <% nav.center do %>
    <% if user_signed_in? %>
      <%= recording_studio_root_switch_dropdown(style: :ghost, size: :md) %>
    <% end %>
  <% end %>

  <% nav.right do %>
    <% if user_signed_in? %>
      <% if recording_studio_accessible_my_access_visible? %>
        <%= render FlatPack::Button::Component.new(
          text: "My access",
          style: :ghost,
          size: :sm,
          href: recording_studio_accessible_my_access_url,
          class: "mr-2"
        ) %>
      <% end %>

      <%= render FlatPack::Avatar::Component.new(
        name: current_user.try(:name).presence || current_user.email,
        size: :sm
      ) %>
    <% end %>
  <% end %>
<% end %>
