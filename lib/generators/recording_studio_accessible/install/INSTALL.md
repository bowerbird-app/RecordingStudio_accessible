RecordingStudioAccessible install complete.

Next steps:

1. Review config/initializers/recording_studio_accessible.rb.
2. Run `bin/rails generate recording_studio_accessible:migrations` if your RecordingStudio version does not already provide access tables.
3. Mount `RecordingStudioAccessible::Engine` only if you want the optional addon status/demo page.
