==============================================================================

Recording Studio Accessible has been installed successfully.

Next steps:
1. Review config/initializers/recording_studio_accessible.rb.
2. Run `bin/rails generate recording_studio_accessible:migrations` if your RecordingStudio version no longer ships access migrations.
3. Run `bin/rails tailwindcss:build` if your host app uses Tailwind CSS.
4. Visit the mounted addon route to verify the access demo/status page.

==============================================================================
